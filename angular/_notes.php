// ! just using a PHP file for better comments

// @ information
npm install -g @angular/cli // * installed angular CLI globally for quick project builds

// ? Commands
ng new app-name // * spins up a new project
ng serve // * spin up live server
npm install -g @angular/cli // * update to the newest CLI for angular
npm install --save @angular/material // * installing material package
ng add @angular/material // * installing material package in the angular CLI + configuration files
ng generate component name-of-component (or) ng g c name-of-component // * Generates a new angular component *** really nice!!!
    
