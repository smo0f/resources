<?php
    // character remover
    // remove all character
    // return new word along with array of uneak characters


     // substr(string $string, int $offset, ?int $length = null): string
        // strlen(string $string): int
        // $stringArray = str_split($string, 1);
        // $stringCount = count($stringArray);
?>